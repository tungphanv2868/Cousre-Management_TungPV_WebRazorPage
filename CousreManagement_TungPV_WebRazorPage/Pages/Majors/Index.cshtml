@page
@model CousreManagement_TungPV_WebRazorPage.Pages.Majors.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Major</h1>
<form method="post" class="spaceBody">
    Search id:
    <input type="text" name="SearchKey" />
    <input type="submit" value="Search" />
</form>
<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Major[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Major[0].Decription)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Major) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Decription)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
